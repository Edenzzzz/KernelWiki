diff --git a/quack/gemm_interface.py b/quack/gemm_interface.py
index 2884dd3a..b399a663 100644
--- a/quack/gemm_interface.py
+++ b/quack/gemm_interface.py
@@ -989,13 +989,14 @@ def gemm_symmetric_out(
     tile_count_semaphore = (
         torch.zeros(1, dtype=torch.int32, device=A.device) if dynamic_scheduler else None
     )
+    tile_m = 256 if get_device_capacity(A.device)[0] == 10 else 128
     gemm_symmetric_sm90_sm100(
         A,
         B,
         out if out is not None else None,
         C if C is not None else None,
         tile_count_semaphore,
-        tile_M=128,
+        tile_M=tile_m,
         tile_N=256,
         cluster_M=2,
         cluster_N=1,
@@ -1428,6 +1429,7 @@ def gemm_symmetric_out_fake(
     if not COMPILE_ONLY or isinstance(A.shape[0], torch.SymInt):
         return
     # gemm_symmetric is not autotuned, compile the single fixed config directly
+    tile_m = 256 if get_device_capacity(A.device)[0] == 10 else 128
     try:
         gemm_symmetric_sm90_sm100(
             A.unsqueeze(0) if A.ndim == 2 else A,
@@ -1435,7 +1437,7 @@ def gemm_symmetric_out_fake(
             out.unsqueeze(0) if out.ndim == 2 else out,
             (C.unsqueeze(0) if C.ndim == 2 else C) if C is not None else None,
             torch.zeros(1, dtype=torch.int32, device=A.device) if dynamic_scheduler else None,
-            tile_M=128,
+            tile_M=tile_m,
             tile_N=256,
             cluster_M=2,
             cluster_N=1,
diff --git a/tests/test_gemm_symmetric.py b/tests/test_gemm_symmetric.py
index f3ea75bb..17af2bdb 100644
--- a/tests/test_gemm_symmetric.py
+++ b/tests/test_gemm_symmetric.py
@@ -49,4 +49,4 @@ def test_symmetric_gemm(dtype, L, M, K, has_C, alpha, beta):
         alpha=alpha,
     )
 
-    assert (out - out_ref).abs().max() < (out_pt - out_ref).abs().max() + 1e-6
+    assert (out - out_ref).abs().max() < 2 * (out_pt - out_ref).abs().max() + 1e-6
