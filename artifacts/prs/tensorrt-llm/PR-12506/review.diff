diff --git a/tensorrt_llm/_torch/cute_dsl_kernels/blackwell/top_k/filtered_top_k_decode_varlen.py b/tensorrt_llm/_torch/cute_dsl_kernels/blackwell/top_k/filtered_top_k_decode_varlen.py
index 787d9ba4ffef..66b79c6986f8 100644
--- a/tensorrt_llm/_torch/cute_dsl_kernels/blackwell/top_k/filtered_top_k_decode_varlen.py
+++ b/tensorrt_llm/_torch/cute_dsl_kernels/blackwell/top_k/filtered_top_k_decode_varlen.py
@@ -24,6 +24,7 @@
 from cutlass.torch import dtype as torch_dtype
 from cutlass.utils.distributed import atomicAdd
 
+from ..utils import TRTLLM_ENABLE_PDL, griddepcontrol_launch_dependents, griddepcontrol_wait
 from .block_scan import block_prefix_sum_kernel
 from .filtered_top_k_varlen_util import (
     FilteredTopKKernelVarlen,
@@ -310,6 +311,8 @@ def filtered_topk_kernel(
         min_blocks_per_mp: cutlass.Constexpr[int] = 1,
     ):
         """CuTe DSL implementation of TopK kernel based on radix-based filter algorithm."""
+        griddepcontrol_wait()
+
         smem = utils.SmemAllocator()
         # TODO: how to simplify the smem allocate codes?
         s_histogram_buf_layout = cute.make_ordered_layout((self.radix + 1), order=(0))
@@ -557,6 +560,8 @@ def filtered_topk_kernel(
                     )
                 work_remaining = has_work
 
+        griddepcontrol_launch_dependents()
+
     @cute.jit
     def __call__(
         self,
@@ -608,6 +613,7 @@ def __call__(
             grid=blocks,
             block=(tiled_copy.size, 1, 1),
             stream=stream,
+            use_pdl=TRTLLM_ENABLE_PDL,
         )
         return
 
diff --git a/tensorrt_llm/_torch/cute_dsl_kernels/blackwell/top_k/single_pass_multi_cta_radix_topk.py b/tensorrt_llm/_torch/cute_dsl_kernels/blackwell/top_k/single_pass_multi_cta_radix_topk.py
index 4ccbaa414f8a..a277bf6439e5 100644
--- a/tensorrt_llm/_torch/cute_dsl_kernels/blackwell/top_k/single_pass_multi_cta_radix_topk.py
+++ b/tensorrt_llm/_torch/cute_dsl_kernels/blackwell/top_k/single_pass_multi_cta_radix_topk.py
@@ -27,6 +27,7 @@
 from cutlass.utils.distributed import atomicAdd
 from cutlass.utils.smem_allocator import SmemAllocator
 
+from ..utils import TRTLLM_ENABLE_PDL, griddepcontrol_launch_dependents, griddepcontrol_wait
 from .block_scan import block_prefix_sum_kernel
 from .filtered_top_k_varlen_util import float_as_uint32, half_as_ushort
 
@@ -1066,6 +1067,8 @@ def single_pass_multi_cta_topk_kernel(
         num_groups = grid_size // ctas_per_group
         num_rows = input_data.shape[0]
 
+        griddepcontrol_wait()
+
         # ---- Shared memory allocation ----
         smem = SmemAllocator()
 
@@ -1367,6 +1370,8 @@ def single_pass_multi_cta_topk_kernel(
                         state_base_ptr + cutlass.Int32(_ARRIVAL_COUNTER), cutlass.Int32(0)
                     )
 
+        griddepcontrol_launch_dependents()
+
     # ------------------------------------------------------------------
     # Host-side launcher
     # ------------------------------------------------------------------
@@ -1395,4 +1400,5 @@ def __call__(
             grid=(total_ctas, 1, 1),
             block=(self.num_threads, 1, 1),
             stream=stream,
+            use_pdl=TRTLLM_ENABLE_PDL,
         )
diff --git a/tensorrt_llm/_torch/cute_dsl_kernels/blackwell/top_k/single_pass_multi_cta_radix_topk_cluster.py b/tensorrt_llm/_torch/cute_dsl_kernels/blackwell/top_k/single_pass_multi_cta_radix_topk_cluster.py
index cf14d21fa5fd..2cccee734210 100644
--- a/tensorrt_llm/_torch/cute_dsl_kernels/blackwell/top_k/single_pass_multi_cta_radix_topk_cluster.py
+++ b/tensorrt_llm/_torch/cute_dsl_kernels/blackwell/top_k/single_pass_multi_cta_radix_topk_cluster.py
@@ -36,6 +36,7 @@
 from cutlass.utils.hardware_info import HardwareInfo
 from cutlass.utils.smem_allocator import SmemAllocator
 
+from ..utils import TRTLLM_ENABLE_PDL, griddepcontrol_launch_dependents, griddepcontrol_wait
 from .single_pass_multi_cta_radix_topk import SinglePassMultiCTARadixTopKKernel, st_release_gpu
 
 
@@ -374,6 +375,8 @@ def single_pass_multi_cta_topk_kernel(
         num_groups = grid_size // ctas_per_group
         num_rows = input_data.shape[0]
 
+        griddepcontrol_wait()
+
         # ---- Shared memory allocation ----
         smem = SmemAllocator()
 
@@ -557,6 +560,8 @@ def single_pass_multi_cta_topk_kernel(
                 if tidx == 0:
                     st_release_gpu(output_counter_ptr, cutlass.Int32(0))
 
+        griddepcontrol_launch_dependents()
+
     # ------------------------------------------------------------------
     # Host-side launcher (override: adds cluster= launch parameter)
     # ------------------------------------------------------------------
@@ -586,4 +591,5 @@ def __call__(
             block=(self.num_threads, 1, 1),
             cluster=(ctas_per_group, 1, 1) if cutlass.const_expr(ctas_per_group > 1) else None,
             stream=stream,
+            use_pdl=TRTLLM_ENABLE_PDL,
         )
