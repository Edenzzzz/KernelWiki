diff --git a/pyproject.toml b/pyproject.toml
index a5019118..0f167320 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -7,14 +7,14 @@ name = "quack-kernels"
 dynamic = ["version"]
 requires-python = ">=3.10"
 dependencies = [
-    "nvidia-cutlass-dsl>=4.4.0",
+    "nvidia-cutlass-dsl>=4.4.1",
     "torch",
     "apache-tvm-ffi>=0.1.6,<0.2",
     "torch-c-dlpack-ext",
 ]
 
 [project.optional-dependencies]
-cu13 = ["nvidia-cutlass-dsl[cu13]>=4.4.0"]
+cu13 = ["nvidia-cutlass-dsl[cu13]>=4.4.1"]
 dev = [
     "pre-commit",
     "ruff",
@@ -45,4 +45,4 @@ ignore = [
     "E731",  # do not assign a lambda expression, use a def
     "E741",  # Do not use variables named 'I', 'O', or 'l'
     "F841",  # local variable is assigned to but never used
-]
\ No newline at end of file
+]
diff --git a/quack/utils.py b/quack/utils.py
index 892d91b4..5a7fcd52 100644
--- a/quack/utils.py
+++ b/quack/utils.py
@@ -126,9 +126,18 @@ def store_shared_remote_x4(
 
 @dsl_user_op
 def fmin(a: Union[float, Float32], b: Union[float, Float32], *, loc=None, ip=None) -> Float32:
+    if cutlass.const_expr(cutlass.CUDA_VERSION.major) == 12:
+        return Float32(
+            nvvm.fmin(
+                T.f32(),
+                Float32(a).ir_value(loc=loc, ip=ip),
+                Float32(b).ir_value(loc=loc, ip=ip),
+                loc=loc,
+                ip=ip,
+            )
+        )
     return Float32(
-        nvvm.fmin(
-            T.f32(),
+            nvvm.fmin(
             Float32(a).ir_value(loc=loc, ip=ip),
             Float32(b).ir_value(loc=loc, ip=ip),
             loc=loc,
