diff --git a/quack/gemm.py b/quack/gemm.py
index 0b3d6b5..6774d96 100644
--- a/quack/gemm.py
+++ b/quack/gemm.py
@@ -150,6 +150,7 @@ def gemm(
     cluster_M: int,
     cluster_N: int,
     cluster_K: int = 1,
+    tile_K: int | None = None,
     pingpong: bool = False,
     persistent: bool = True,
     is_dynamic_persistent: bool = False,
@@ -190,6 +191,8 @@ def gemm(
 
     device_capacity = get_device_capacity(A.device)
     assert device_capacity[0] in [9, 10, 11, 12], "Only SM90, SM100, SM110, and SM120 are supported"
+    if tile_K is not None:
+        assert device_capacity[0] in [10, 11], "tile_K currently requires SM100/SM110"
     if use_tma_gather:
         assert device_capacity[0] in [10, 11], "TMA gather currently requires SM100/SM110"
     if rounding_mode == RoundingMode.RS:
@@ -220,7 +223,7 @@ def gemm(
         b_major,
         d_major,
         c_major,
-        (tile_M, tile_N),
+        (tile_M, tile_N, tile_K) if tile_K is not None else (tile_M, tile_N),
         (cluster_M, cluster_N, cluster_K),
         pingpong,
         persistent,
diff --git a/quack/gemm_act.py b/quack/gemm_act.py
index 1be9491..a3f21a4 100644
--- a/quack/gemm_act.py
+++ b/quack/gemm_act.py
@@ -384,6 +384,7 @@ def gemm_act(
     tile_N: int,
     cluster_M: int,
     cluster_N: int,
+    tile_K: int | None = None,
     pingpong: bool = False,
     persistent: bool = True,
     is_dynamic_persistent: bool = False,
@@ -436,6 +437,8 @@ def gemm_act(
 
     device_capacity = get_device_capacity(A.device)
     assert device_capacity[0] in [9, 10, 11, 12], "Only SM90, SM100, SM110, and SM120 are supported"
+    if tile_K is not None:
+        assert device_capacity[0] in [10, 11], "tile_K currently requires SM100/SM110"
     if rounding_mode == RoundingMode.RS:
         assert device_capacity[0] == 10, "Stochastic rounding (RoundingMode.RS) requires SM100"
 
@@ -459,7 +462,7 @@ def gemm_act(
         d_major,
         c_major,
         postact_major,
-        (tile_M, tile_N),
+        (tile_M, tile_N, tile_K) if tile_K is not None else (tile_M, tile_N),
         (cluster_M, cluster_N, 1),
         pingpong,
         persistent,
diff --git a/quack/gemm_dact.py b/quack/gemm_dact.py
index d7ae81c..71d6402 100644
--- a/quack/gemm_dact.py
+++ b/quack/gemm_dact.py
@@ -367,6 +367,7 @@ def gemm_dact(
     tile_N: int,
     cluster_M: int,
     cluster_N: int,
+    tile_K: int | None = None,
     pingpong: bool = True,
     persistent: bool = True,
     is_dynamic_persistent: bool = False,
@@ -431,6 +432,8 @@ def gemm_dact(
 
     device_capacity = get_device_capacity(A.device)
     assert device_capacity[0] in [9, 10, 11, 12], "Only SM90, SM100, SM110, and SM120 are supported"
+    if tile_K is not None:
+        assert device_capacity[0] in [10, 11], "tile_K currently requires SM100/SM110"
 
     if is_dynamic_persistent and device_capacity[0] == 9:
         assert tile_count_semaphore is not None, (
@@ -449,7 +452,7 @@ def gemm_dact(
         d_major,
         c_major,
         postact_major,
-        (tile_M, tile_N),
+        (tile_M, tile_N, tile_K) if tile_K is not None else (tile_M, tile_N),
         (cluster_M, cluster_N, 1),
         pingpong,
         persistent,
diff --git a/quack/gemm_interface.py b/quack/gemm_interface.py
index e1994d5..ba346ca 100644
--- a/quack/gemm_interface.py
+++ b/quack/gemm_interface.py
@@ -261,7 +261,7 @@ def gemm_tuned(
         config.cluster_m,
         config.cluster_n,
         config.cluster_k,
-        config.pingpong,
+        pingpong=config.pingpong,
         persistent=True,
         is_dynamic_persistent=dynamic_scheduler,
         max_swizzle_size=config.max_swizzle_size,
@@ -341,7 +341,7 @@ def gemm_act_tuned(
         config.tile_n,
         config.cluster_m,
         config.cluster_n,
-        config.pingpong,
+        pingpong=config.pingpong,
         persistent=True,
         is_dynamic_persistent=dynamic_scheduler,
         max_swizzle_size=config.max_swizzle_size,
@@ -409,7 +409,7 @@ def gemm_dact_tuned(
         config.tile_n,
         config.cluster_m,
         config.cluster_n,
-        config.pingpong,
+        pingpong=config.pingpong,
         persistent=True,
         is_dynamic_persistent=dynamic_scheduler,
         max_swizzle_size=config.max_swizzle_size,
@@ -1333,7 +1333,7 @@ def gemm_gated_tuned(
         config.tile_n,
         config.cluster_m,
         config.cluster_n,
-        config.pingpong,
+        pingpong=config.pingpong,
         persistent=True,
         is_dynamic_persistent=dynamic_scheduler,
         max_swizzle_size=config.max_swizzle_size,
@@ -1429,7 +1429,7 @@ def gemm_dgated_tuned(
         config.tile_n,
         config.cluster_m,
         config.cluster_n,
-        config.pingpong,
+        pingpong=config.pingpong,
         persistent=True,
         is_dynamic_persistent=dynamic_scheduler,
         max_swizzle_size=config.max_swizzle_size,
@@ -1772,7 +1772,7 @@ def _gemm_rms_tuned(
         config.tile_n,
         config.cluster_m,
         config.cluster_n,
-        config.pingpong,
+        pingpong=config.pingpong,
         persistent=True,
         is_dynamic_persistent=dynamic_scheduler,
         max_swizzle_size=config.max_swizzle_size,
@@ -1953,7 +1953,7 @@ def gemm_norm_act_tuned(
         config.tile_n,
         config.cluster_m,
         config.cluster_n,
-        config.pingpong,
+        pingpong=config.pingpong,
         persistent=True,
         is_dynamic_persistent=dynamic_scheduler,
         max_swizzle_size=config.max_swizzle_size,
@@ -2015,7 +2015,7 @@ def gemm_norm_gated_tuned(
         config.tile_n,
         config.cluster_m,
         config.cluster_n,
-        config.pingpong,
+        pingpong=config.pingpong,
         persistent=True,
         is_dynamic_persistent=dynamic_scheduler,
         max_swizzle_size=config.max_swizzle_size,
diff --git a/quack/gemm_norm_act.py b/quack/gemm_norm_act.py
index ab80163..b61a8b1 100644
--- a/quack/gemm_norm_act.py
+++ b/quack/gemm_norm_act.py
@@ -277,6 +277,7 @@ def gemm_norm_act_fn(
     tile_N: int,
     cluster_M: int,
     cluster_N: int,
+    tile_K: int | None = None,
     pingpong: bool = False,
     persistent: bool = True,
     is_dynamic_persistent: bool = False,
@@ -327,6 +328,8 @@ def gemm_norm_act_fn(
 
     device_capacity = get_device_capacity(A.device)
     assert device_capacity[0] in [9, 10, 11, 12], "Only SM90, SM100, SM110, and SM120 are supported"
+    if tile_K is not None:
+        assert device_capacity[0] in [10, 11], "tile_K currently requires SM100/SM110"
     if rounding_mode == RoundingMode.RS:
         assert device_capacity[0] == 10, "Stochastic rounding requires SM100"
 
@@ -349,7 +352,7 @@ def gemm_norm_act_fn(
         d_major,
         c_major,
         postact_major,
-        (tile_M, tile_N),
+        (tile_M, tile_N, tile_K) if tile_K is not None else (tile_M, tile_N),
         (cluster_M, cluster_N, 1),
         pingpong,
         persistent,
diff --git a/quack/gemm_sm100.py b/quack/gemm_sm100.py
index b1fad72..b680407 100644
--- a/quack/gemm_sm100.py
+++ b/quack/gemm_sm100.py
@@ -188,7 +188,10 @@ def __init__(
         """
 
         self.acc_dtype: Type[cutlass.Numeric] = acc_dtype
-        self.use_2cta_instrs = mma_tiler_mn[0] in (256,)
+        self.sf_vec_size = sf_vec_size
+        self.blockscaled = sf_vec_size is not None
+        valid_2cta_m = (128, 256) if not self.blockscaled else (256,)
+        self.use_2cta_instrs = cluster_shape_mnk[0] % 2 == 0 and mma_tiler_mn[0] in valid_2cta_m
         self.cluster_shape_mnk = cluster_shape_mnk
         assert cluster_shape_mnk[2] == 1, "Cluster shape K must be 1"
         # K dimension: if user provides 3 values, use their K; otherwise default in _setup_attributes
@@ -196,8 +199,6 @@ def __init__(
             self.mma_tiler = tuple(mma_tiler_mn)
         else:
             self.mma_tiler = (*mma_tiler_mn, 0)
-        self.sf_vec_size = sf_vec_size
-        self.blockscaled = sf_vec_size is not None
         self.is_persistent = True
         self.pingpong = False  # for compatibility with GemmSm90
         self.use_clc_persistence = use_clc_persistence
diff --git a/quack/gemm_sq_reduce.py b/quack/gemm_sq_reduce.py
index f359990..673b31f 100644
--- a/quack/gemm_sq_reduce.py
+++ b/quack/gemm_sq_reduce.py
@@ -192,6 +192,7 @@ def gemm_sq_reduce(
     tile_N: int,
     cluster_M: int,
     cluster_N: int,
+    tile_K: int | None = None,
     pingpong: bool = False,
     persistent: bool = True,
     is_dynamic_persistent: bool = False,
@@ -204,6 +205,8 @@ def gemm_sq_reduce(
     """
     device_capacity = get_device_capacity(A.device)
     assert device_capacity[0] in [9, 10, 11, 12], "Only SM90, SM100, SM110, and SM120 are supported"
+    if tile_K is not None:
+        assert device_capacity[0] in [10, 11], "tile_K currently requires SM100/SM110"
 
     A_p, B_p, D_p, C_p = perm3d(A, B, D, C)
     a_major, b_major, d_major, c_major = get_majors(A_p, B_p, D_p, C_p)
@@ -223,7 +226,7 @@ def gemm_sq_reduce(
         b_major,
         d_major,
         c_major,
-        (tile_M, tile_N),
+        (tile_M, tile_N, tile_K) if tile_K is not None else (tile_M, tile_N),
         (cluster_M, cluster_N, 1),
         pingpong,
         persistent,
diff --git a/quack/gemm_symmetric.py b/quack/gemm_symmetric.py
index 244ba0f..840cb69 100644
--- a/quack/gemm_symmetric.py
+++ b/quack/gemm_symmetric.py
@@ -306,6 +306,7 @@ def gemm_symmetric(
     tile_N: int,
     cluster_M: int,
     cluster_N: int,
+    tile_K: int | None = None,
     pingpong: bool = False,
     persistent: bool = True,
     is_dynamic_persistent: bool = False,
@@ -326,13 +327,15 @@ def gemm_symmetric(
 
     device_capacity = get_device_capacity(A.device)
     assert device_capacity[0] in [9, 10, 11, 12], "Only SM90, SM100, SM110, and SM120 are supported"
+    if tile_K is not None:
+        assert device_capacity[0] in [10, 11], "tile_K currently requires SM100/SM110"
 
     if is_dynamic_persistent and device_capacity[0] == 9:
         assert tile_count_semaphore is not None, (
             "Dynamic persistent tile scheduler in SM90 requires a semaphore in GMEM"
         )
 
-    tile_shape_mn = (tile_M, tile_N)
+    tile_shape_mn = (tile_M, tile_N, tile_K) if tile_K is not None else (tile_M, tile_N)
     cluster_shape_mnk = (cluster_M, cluster_N, 1)
     alpha_mode = 2 if isinstance(alpha, Tensor) else (1 if alpha != 1.0 else 0)
     beta_mode = 2 if isinstance(beta, Tensor) else (1 if beta != 1.0 else 0)
